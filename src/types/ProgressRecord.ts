export type ProgressRecord = {
    date:string // dia de registro del progreso
    weightKg?: number        // Peso en kilogramos
    heightCm?: number        // Altura en centímetros
    note?:string // notas necesarias sobre los cambios

}